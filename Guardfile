# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'sass', :input => 'css/sass', :output => 'css', :style => :compressed

guard 'livereload' do
  watch(%r{.+\.(css|html|js)$})
end
